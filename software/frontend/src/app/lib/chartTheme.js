/** Recharts styling that follows CSS theme tokens (light + dark). */
export const chartGridProps = {
  strokeDasharray: '3 3',
  stroke: 'var(--color-border)',
};

export const chartAxisTick = {
  fill: 'var(--color-muted)',
  fontSize: 11,
};

export const chartAxisStroke = 'var(--color-muted)';

export const chartTooltipStyle = {
  backgroundColor: 'var(--color-surface-elevated)',
  border: '1px solid var(--color-border)',
  borderRadius: '12px',
  color: 'var(--color-foreground)',
  boxShadow: 'var(--shadow-card-hover)',
};

export const chartLineBrand = 'var(--color-brand-600)';
export const chartLineAccent = 'var(--color-aqi-very-unhealthy)';
